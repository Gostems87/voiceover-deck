# Voiceover-deck — optional cloned-voice setup (ElevenLabs).
# Copy this file into your project root as `.env`, then paste your key in.
# Leave blank to use the built-in browser voice (no account needed).
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
